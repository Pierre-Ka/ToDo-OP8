{% extends 'base.html.twig' %}

{% block header_title %}
    <h1>Bienvenue sur Todo List, l'application vous permettant de gérer l'ensemble de vos tâches sans effort !</h1>
{% endblock %}

{% block body %}
    <h2 class="text-center m-5"> Que souhaitez-vous faire maintenant ?</h2>

    <div class="row mb-5">
        <div class="col-lg-4">
            <a href="{{ path('task_create') }}" class="btn btn-success btn-md">Créer une nouvelle tâche</a>
        </div>
        <div class="col-lg-4">
            <a href="{{ path('task_list_undone') }}" class="btn btn-info btn-md">Consulter la liste des tâches à faire</a>
        </div>
        <div class="col-lg-4">
            <a href="{{ path('task_list_done') }}" class="btn btn-secondary btn-md">Consulter la liste des tâches terminées</a>
        </div>
    </div>
    {% if is_granted('ROLE_ADMIN') %}
        <div class="row mb-5">
            <div class="col-lg-4">
                <a href="{{ path('user_create') }}" class="btn btn-success btn-md">Créer une nouvel utilisateur</a>
            </div>
            <div class="col-lg-4">
                <a href="{{ path('user_list') }}" id="test-for-admin" class="btn btn-info btn-md">Consulter la liste des utilisateurs</a>
            </div>
            <div class="col-lg-4">
            </div>
        </div>
    {% endif %}
{% endblock %}
